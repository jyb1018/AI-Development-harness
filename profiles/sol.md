# GPT-5.6 Sol compatibility profile

Use the same core as Astra. Do not turn historical Luna/Terra/Sol role assignments
into mandatory routing. Retain the user's effective host settings initially;
compare effort settings on representative tasks instead of forcing maximum effort.
Use parallel work only when independent and supported. Avoid duplicating any native
host orchestration with a second mandatory manager/reviewer chain.
Track the parent outcome across commits; a source-only milestone stays distinct
from tested runtime behavior. These are design safeguards, not measured Sol defects.
This profile does not select the model, change permissions, or certify performance.
