# Generic compatibility profile

Use the shared Universal Harness core. Identify the actual model and host only when
needed for tool compatibility or evaluation; do not infer them from this filename.
Use available tools, not imagined APIs. Keep host permissions and project safeguards.
Use task-sized validation and a real completion condition. Do not load other profiles.
This is guidance, not a runtime configuration or a performance certification.
