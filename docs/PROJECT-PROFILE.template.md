# Optional project profile

Keep only project-specific facts. Prefer links to existing authoritative documents.

- Product outcome and out-of-scope behavior:
- Runtime/framework versions and supported deployment targets:
- Exact local setup, test, lint, build commands:
- Data ownership and invariants that must never change implicitly:
- Disposable test environment, seed/reset path, permitted endpoints:
- External-effect authority and approval requirements:
- Critical journey and observables proving completion:
- Release/rollback owner and limits:

Do not put secrets here. Do not copy the entire universal core or skill catalog.
An unfilled template is not a blocker for a task whose context is already available.
